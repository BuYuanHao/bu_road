1、是否单继承
2、创建一个对象的方法
3、一个类初始化的顺序
4、java中创建一个全局变量
5、常见异常
6、集合List、set、map的区别，线程安全的实现是哪些
7、hashmap的底层原理

场景题
1、如何获取两个list集合的交集
2、user对象（id，idCode，name，age），如何快速转换成map<String,User>
3、user对象（id，idCode，name，age），根据idCode去重


9、泛型的使用
10、如何读入、写出一个txt、excel
11、有没有写过自定义注解

并发
12、线程创建的方式、死锁的条件
13、线程池的参数、工作方式
14、
15、锁的分类、sys和lock的区别、什么是cas、哪里用到cas、aba如何解决
16、CyclicBarrier、CountDownLatch、Semaphore 的用法
17、volatile 关键字的作用、ThreadLocal 作用

场景题
1、如何保证线程T1，T2，T3 顺序执行？
2、如何保证线程最多只有3个线程执行？
3、如何线程自己的变量？



spring
1、spring的特征、核心组件、常见注解
2、spring ioc和aop的原理、aop的实践
3、springmvc的流程
4、默认可以同时处理多少请求
5、日志框架是哪个


jvm
1、判断垃圾、垃圾回收算法
2、如何运行一个jar包、jdk自带的工具有哪些

消息中间件


redis
1、redis的数据类型
2、redis为什么快
3、spring和redis如何结合使用


数据库
1、mysql 的存储引擎是哪个、它有什么特点
2、如何优化索引、索引失效的情形
3、知道什么是触发器吗
4、什么是分库分表、如何切分
5、事务的隔离级别、spring默认是啥
6、CHAR和VARCHAR的区别

场景题
1、告警表 alarm（id（bigint），personId（人员id，char），time（抓拍时间，long）），查询人员告警数量超过10条，前十名人员的id，以及对应数量，和最后抓拍的时间
2、如何批量新增


算法题

给你一个非负整数数组 nums ，你最初位于数组的 第一个下标 。数组中的每个元素代表你在该位置可以跳跃的最大长度。
判断你是否能够到达最后一个下标，如果可以，返回 true ；否则，返回 false 。


